let dayToStay = 0;
let numDays = 1;
let distance = 2;
let priceAverageKm = 35;
    console.log(dayToStay, numDays, distance, price)

function priceTrip (){
    if ( dayToStay < 1000 ?  descuentox 35) {
         else (
            numDays * dayToStay (distance * priceAverageKm)
         )
    }
}
